name: CodeQL

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '29 10 * * 4'

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'javascript' ]

    steps:
      - name: checkout
        uses: actions/checkout@v3.5.0

      # Initializes the CodeQL tools for scanning.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@8c8d71dde4abced210732d8486586914b97752e8 # pin@v2
        with:
          languages: ${{ matrix.language }}

      - name: Autobuild
        uses: github/codeql-action/autobuild@8c8d71dde4abced210732d8486586914b97752e8 # pin@v2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@8c8d71dde4abced210732d8486586914b97752e8 # pin@v2
